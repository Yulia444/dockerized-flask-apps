pipeline {
    agent any
    stage('Setting the variables values') {
        steps {
            sh '''
                #!/bin/bash
                echo "hello world"
            '''
        }
    }
}